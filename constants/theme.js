import { COLORS } from "./colors";
import { FONTS } from "./fonts";
import { SIZES } from "./sizes";

// Single Central Theme Export
const appTheme = { COLORS, FONTS, SIZES };

export { COLORS, FONTS, SIZES };
export default appTheme;
